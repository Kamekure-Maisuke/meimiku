# PostgREST Configuration
# https://docs.postgrest.org/en/v14/references/configuration.html

# Database connection
db-uri = "postgres://meimiku:meimiku@localhost:5432/meimiku"
db-schemas = "api"
db-anon-role = "web_anon"

# JWT settings
jwt-secret = "my-super-secret-jwt-key-for-meimiku-2024"
app.settings.jwt_secret = "my-super-secret-jwt-key-for-meimiku-2024"

# Server settings
server-port = 3000

# Optional: Performance tuning (uncomment to enable)
# db-pool = 10
# db-pool-acquisition-timeout = 10
# max-rows = 1000

# Optional: Logging (uncomment to enable)
# log-level = "info"
